if SHIELD_TURING_LEFT

config ZMK_KEYBOARD_NAME
    default "Turing"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_TURING_LEFT || SHIELD_TURING_RIGHT

config ZMK_SPLIT
    default y

endif